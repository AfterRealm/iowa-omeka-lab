# Omeka S API credentials for populate.py / create_site.py
# Generate via: log into /admin, go to user profile, create an API key.
# Or run the bootstrap snippet in README.md to insert one directly into the DB.

OMEKA_API_BASE=http://localhost:8090/api
OMEKA_KEY_ID=
OMEKA_KEY_CRED=
